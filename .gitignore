# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where 3rd-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Temporary files, for example, from tests.
/tmp/

# Ignore package tarball (built via "mix hex.build").
bank-*.tar

# Ignore assets that are produced by build tools.
/priv/static/assets/

# Ignore digested assets cache.
/priv/static/cache_manifest.json

# In case you use Node.js/npm, you want to ignore these.
npm-debug.log
/assets/node_modules/
/chain_adapter/node_modules/

# npm cache + logs that land at the repo root when sandboxes
# reroute HOME (e.g. when tools point npm at the project tree).
/.npm/

# macOS Finder metadata.
.DS_Store

# Python build / cache artifacts (sdks/mcp/).
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/

# Local env files (never commit secrets). The *.example files are tracked.
.env
.env.*
!.env.example
!.env.*.example
/chain_adapter/.env
/chain_adapter/.env.*
!/chain_adapter/.env.example
!/chain_adapter/.env.*.example

# Claude Code session data + temp worktrees. Not part of the app.
/.claude/

# Local-only ops scratch (worker prompts/reports/backups). Not shipped.
/control-tower/
